# 生活栏目示例数据

- `lib/music.ts`：全站音乐歌单。替换标题、作者、音频路径、时长和说明即可；音乐页和全局播放器共用这一份数据。
- `public/audio/*.wav`：本次通过 `scripts/generate-demo-audio.py` 的振荡器合成生成，未使用第三方旋律文件、录音或采样。每首 48 秒，单声道 PCM WAV。不是来自外部音乐站点，也不冒充真实艺人作品。
- `public/audio/instrumental.vtt`：无人声音乐的文字说明。
- `lib/life-content.ts`：电影、播客、旅行、爱好的 AI 虚构示例。电影不是已发行影片，播客只有文字稿而无节目音频，旅行不是实际出行攻略。现有配图沿用项目示例资产。
- 替换为真实内容时，同步更新页面的示例提示；使用第三方音频或图片时，填写真实来源及适用授权，不能把“可访问”当作可自由使用。

播放器只在用户点击后启动，默认音量为 50%；站内客户端导航共用根布局中的音频元素，刷新后不自动播放。当前未接后台或存储个人播放记录。
